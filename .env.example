# ============================================================================
# BizTrix CRM v2.0 - Coolify Environment Configuration TEMPLATE
# ============================================================================
# Copy these variables into Coolify dashboard → Environment Variables
# Replace values with your actual configuration
# ============================================================================

# Service URLs (for Coolify routing/domain setup)
SERVICE_URL_BACKEND=api.tokocrypto.live
SERVICE_FQDN_BACKEND=
SERVICE_URL_FRONTEND=tokocrypto.live
SERVICE_FQDN_FRONTEND=

# Supabase URLs (Mark as: BUILDTIME + RUNTIME ✅)
VITE_SUPABASE_URL=https://tdqljwenzuptupjihsvg.supabase.co

# Supabase Anon Key - for frontend auth (Mark as: BUILDTIME + RUNTIME ✅)
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase Service Role Key - KEEP SECRET! (Mark as: RUNTIME ONLY ✅)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# API URL for docker containers (Mark as: BUILDTIME + RUNTIME ✅)
VITE_API_URL=http://backend:3001

# CORS Origin (Mark as: RUNTIME ONLY ✅)
CORS_ORIGIN=*

# ============================================================================
# COOLIFY SETUP INSTRUCTIONS:
# ============================================================================
#
# Step 1: Delete these if they exist in Coolify:
#         ❌ SERVICE_FQDN_BACKEND
#         ❌ SERVICE_FQDN_FRONTEND
#         ❌ NODE_ENV (buildtime)
#
# Step 2: ADD all above variables in Coolify dashboard with flags:
#
#         BUILDTIME + RUNTIME (check both):
#         ✓ VITE_SUPABASE_URL
#         ✓ VITE_SUPABASE_ANON_KEY
#         ✓ VITE_API_URL
#
#         RUNTIME ONLY:
#         ✓ SUPABASE_SERVICE_ROLE_KEY
#         ✓ CORS_ORIGIN
#         ✓ SERVICE_URL_BACKEND
#         ✓ SERVICE_URL_FRONTEND
#
# Step 3: Click "Force Deploy"
#
# Step 4: Verify:
#         GET https://api.tokocrypto.live/health
#         GET https://tokocrypto.live
#
# ============================================================================
