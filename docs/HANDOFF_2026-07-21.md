# Handoff — 2026-07-21 (QA department + dialer reconciliation)

Session summary so a new chat can continue cleanly. All changes are on `main`.

## Shipped this session (commits)
- **QA Live full-day count** (`42c4a2e`, `5190c2d`) — `/qa/live` no longer truncates a busy day; LiveTab defaults to the whole **business day** (US Eastern, DST-safe) with a Today / Last 4h / 48h toggle.
- **Two-tier QA org** (`85c098b`, **migration `208_qa_teams.sql`**) — compliance wires managers↔companies↔agents only; the manager owns methods + tasks + per-company review config. `qa_manager_companies` (one manager/company), `qa_team_members` (one manager/agent). Revokes `view_all_qa_reviews` from the global qa_manager role so managers are scoped.
- **Agent method-gating** (`9444aff`) — a qa_agent sees only the work-type sections their manager checked, in Live + My tasks.
- **Completed edit + comments** (`4b55cd3`) — Completed records are clickable → open the review editor (notes + edit history); managers edit any, owning agent edits their own until finalized.
- **Company-config ↔ agent-methods coherence** (`9d420e9`) — the company's Review-type toggles gate the per-agent method checkboxes (locked when off); RCM sample % + card fields explained.
- **Agent pending badge fix** (`6ab776e`) — `/qa/my-companies` now counts only the agent's own tasks (was counting the whole company pool → "321 that won't clear"); "· QA off" label → "· no review types on"; turning a review type off purges its unassigned pool.
- **Dialer→CRM attribution fix** (`840615f`) — `resolveAgent` is now deterministic when a dialer id is shared across duplicate profiles (was nondeterministic → cross-company count drift).
- **Reports redesign** (`38b686b`) — line charts render at a fixed 160px height (were ballooning on the full-width shell); ReportsTab is max-width + sectioned (KPIs → Trends → Quality breakdown → People); People split into **Closers / Fronters / QA agents** charts.

## Pending (needs the user / next session)
1. **Apply `backend/migrations/208_qa_teams.sql`** in the Supabase SQL editor — the two-tier scoping + team-only method binding reads those tables. (Supabase MCP was unauthenticated this session.)
2. **De-duplicate shared-dialer-id `user_profiles`** (Muhammad Taha EasyTech vs 1-Vertex; M. Mohsin no-company dup; Danish Waris; the `TMC100589` two-person collision; 18 profiles with ids but no company). Then the assistant can **re-attribute today's already-recorded transfers** + dedupe duplicate transfer rows so counts match the dialers exactly. Details in memory `vicidial_agent_attribution`.
3. **NEXT TASK — read-only admin governance** (see the prompt the user has). Superadmin fully controls what `readonly_admin` can see / download, from the superadmin dashboard; readonly keeps the same AdminPanel look.

## Key references
- Dialers (CEO/CEO web login): WaveTech `wavetechnew.i5.tel` (WTI), EasyTech/ETC `wavetech3new.i5.tel` (ETC). Ground-truth XFER = `AST_agent_performance_detail.php?...report_display_type=TEXT`.
- Memory files: `qa_two_tier_org`, `qa_ux_reporting_2026_07`, `vicidial_agent_attribution`, `qa_command_center`, `qa_department`.
